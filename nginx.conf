##DEV##
# server {
#   listen 80;
#   server_name _;

#   root /usr/share/nginx/html;
#   index index.html;

#   # App static files
#   location / {
#     try_files $uri $uri/ /index.html;
#   }

#   # Proxy API to the backend service
#   location /api/ {
#     proxy_pass http://app:9000/;  # <— KEY: use service name "app", not localhost
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
  
#   # Optional: lightweight health endpoint for the web container
#   location = /healthz {
#     return 200 'ok';
#     add_header Content-Type text/plain;
#   }
# }

##PROD##
server {
  listen 80;
  server_name _;


    root /usr/share/nginx/html;
    index index.html;

    # Handle React Router routes properly
    location / {
        try_files $uri /index.html;
    }

  # Proxy API under /app/api/* to Spring backend
  location ^~ /api/ {
    # keep the /api path for the backend
    proxy_pass http://solarcom-app:8082/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  # Optional: cache headers for assets
  location ~* \.(js|css|png|jpg|jpeg|gif|svg|woff2?)$ {
    expires 30d;
    access_log off;
  }
}
