import * as React from "react";
import Box from "@mui/material/Box";
import {
  Grid,
  List,
  ListItem,
  ListItemButton,
  ListItemText,
} from "@mui/material";
export default function Climatisation() {
  return (
    <Box sx={{ width: "100%" }}>
      <Box sx={{}}></Box>
    </Box>
  );
}
